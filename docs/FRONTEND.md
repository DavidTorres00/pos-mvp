# FRONTEND.md

# Frontend

## Arquitectura

```
src/
 app/
 components/
 features/
   auth/
   caja/
   categorias/
   compras/
   configuracion/
   equipos/
   inventario/
   ordenes-reorden/
   productos/
   proveedores/
   reglas-reorden/
   subcategorias/
   sucursales/
   usuarios/
   ventas/
 hooks/
 services/
 stores/
 pages/
 layouts/
```

## Principios
- Feature-based.
- Componentes reutilizables.
- UI consistente.
- Consumir únicamente la API oficial.

## Estado Global
- Zustand

## Datos remotos
- TanStack Query

## Formularios
- React Hook Form + Zod

## Flujo
Pantalla → Hook → Service(API) → Backend

## Calidad
- Sin componentes huérfanos.
- Sin lógica compleja en componentes.
- Componentes pequeños y reutilizables.

## Seguridad y roles
- `authStore.usuario.role` (`admin` | `cajero`) refleja el rol que devuelve el backend; también trae `puede_retirar_excedente` (gatea la acción de retiro de excedente en `VentaKiosco` junto con el rol). `usuario` ya no es solo el snapshot del login: `useMe` (`features/auth/hooks/useMe.ts`, `GET /auth/me`) se monta en `ProtectedLayout` con `refetchInterval: 30_000` y resincroniza `setSession` — sin esto, un cambio de permiso/estado que el admin hace en vivo (ej. otorgar `puede_retirar_excedente`) nunca le llegaba a una sesión de cajero ya abierta.
- `services/api.ts`: interceptor de request agrega header `X-CSRF-Token` leyendo la cookie `csrf_token` (requerido por el backend en todo método mutante).
- `ProtectedLayout` bifurca por completo según rol, no solo filtra un nav compartido: el admin ve el sidebar clásico (`NAV_GROUPS`, sin ningún campo de filtrado por rol — ver nota abajo). El `cajero` nunca llega a ese código: tiene su propio chrome tipo kiosko (topbar `bg-primary`, sin sidebar, `h-svh overflow-hidden` para que solo haga scroll lo que gestiona su propio `overflow-y-auto` interno) con dos accesos — "Ventas" (`NavLink` real, su pantalla principal) y "Productos" (no navega, abre un `Sheet` panel lateral desde la derecha — ver Ventas/Caja abajo). `NAV_GROUPS` ya no tiene campos `adminOnly`/`cajeroOnly`: como el cajero corta camino antes con su propio chrome, ese filtro solo lo ejecutaba el admin y nunca filtraba nada (`isAdmin` siempre `true` en ese punto) — se limpió el campo muerto en vez de dejarlo evaluando un no-op.
- Páginas admin-only (Categorías, Inventario, Compras, Proveedores, Reglas/Órdenes de reorden, Reportes, Auditoría, Usuarios, Sucursales, Configuración) siguen el patrón: consulta gateada con `enabled: isAdmin` + estado "No tienes acceso a este módulo" para quien no sea admin — vestigio de cuando el cajero navegaba por el mismo sidebar; ya no es alcanzable desde su chrome, pero se deja como cinturón de seguridad server-adjacent. Excepción: `DashboardPage`, que ya no gatea por rol — el cajero nunca la visita (ver login abajo), así que se simplificó a una página puramente admin.
- Toggle activo/inactivo (`Categorias`/`Productos`/`Proveedores`/`ReglasReorden`Table): siempre pide confirmación vía `AlertDialog` antes de disparar la mutación. El mismo patrón se usa para "Aprobar y pagar" en `OrdenesReordenTable`, dado que mueve dinero real e irreversible.

## Paginación
Server-side: cada servicio de listado (`producto`, `categoria`, `subcategoria`, `compra`, `venta`, `proveedor`, `reglaReorden`, `ordenReorden`, `usuario`, `auditoria`, `inventario`, `caja`, `sucursal`, `equipo`) recibe `{ page, size, ...filtros }` y devuelve `PaginatedResponse<T>` (`services/pagination.ts`) — `{items, total, page, size}`. `usePagination` solo trackea `page`/`size` local; la página lee `items`/`total` de la respuesta y se lo pasa a `Pagination.tsx`.

## Patrón de listados (`TableCard`)
Toda página o dialog con tabla (`Productos`, `Categorías`, `Proveedores`, `Reglas/Órdenes de reorden`, `Compras`, `Ventas`, `Usuarios`, `Auditoría`, `Inventario`, y los dialogs "Movimientos"/"Ventas del día" de `VentaKiosco`) sigue el mismo esqueleto para evitar repetir el bloque loading/error/paginación en cada uno:
- `components/TableCard.tsx`: envuelve la tabla en un card (`rounded-xl border bg-card shadow-sm`), muestra `LoadingState`/`ErrorState` mientras carga, y solo pinta el pie de paginación si `pageCount > 1` (antes había un bug donde el `div` de paginación quedaba vacío-pero-con-padding cuando `Pagination` devolvía `null` con una sola página — corregido ahí, una vez, para las ~12 páginas que lo usan).
- `EmptyState`/`ErrorState` (`components/DataStates.tsx`) aceptan `bordered?: boolean` (default `true`): en `false` cuando viven dentro de un `TableCard` (que ya tiene su propio borde), evita el doble marco.
- Cada `XTable.tsx` acepta `emptyMessage?: string` — la página decide el texto ("No hay X." vs "No hay X que coincidan con tu búsqueda.") según si hay algún filtro activo.
- Buscador: `Input` con `w-full max-w-sm` dentro de un contenedor `flex-1` (crece hasta ese tope, no infinito) — no `max-w-xs` fijo. Botón **"Limpiar filtros"** (ghost) aparece solo si `hayFiltrosActivos` es verdadero, resetea todos los filtros de esa página de un clic.
- `components/ui/table.tsx`: `TableHeader` lleva `bg-muted/50` — encabezados de columna con fondo gris sutil en toda la app, un solo lugar.
- Ancho de página: `w-full` (no `max-w-7xl` ni ningún cap fijo) en las páginas de tabla/lista y en el Dashboard — se adapta a cualquier monitor sin dejar espacio muerto a la derecha. Páginas de solo-mensaje ("No tienes acceso") o formulario de un campo se quedan angostas (`max-w-2xl`/`max-w-sm`) a propósito, por legibilidad.

## Impresión (voucher de retiro de excedente)
No se agregó ninguna librería de PDF/tickets: `ComprobanteRetiro` (`features/caja/components/`) renderiza el comprobante en un `div#voucher-print` — extraído de `VoucherRetiroDialog` para reusarlo tal cual también en el `Sheet` de éxito de `VentaKiosco` (antes cada uno tenía su propia copia del markup del voucher). El botón "Imprimir" llama a `window.print()`. El aislamiento (que solo se imprima el voucher, no el resto de la app) se resuelve con una regla `@media print` global en `index.css` (`visibility: hidden` en todo excepto `#voucher-print`, más `max-width: 320px` para que se vea angosto como un ticket en cualquier impresora — no asume hardware térmico real conectado), no con una librería nueva.
- `ComprobanteRetiro` incluye `autorizado_por` (nuevo campo en `VoucherRetiroOut`, relationship `MovimientoCaja.usuario`/`usuario_nombre` igual que `CajaSesion`): quién ejecutó el retiro, no quién es dueño de la caja. Solo se muestra si difiere de `cajero` (comparación de nombres — sin un caso de negocio que justifique compararlos por id todavía) — si el propio cajero se retira a sí mismo, esa fila sería redundante.
- `lib/numeroALetras.ts`: conversor número→palabras en español para el monto del comprobante ("MIL NOVECIENTOS PESOS 00/100 M.N.", estilo cheque), sin acentos en mayúsculas (convención de los documentos bancarios de referencia). Soporta hasta millones; sin librería externa.

## Estado
Roadmap completo implementado, incluida la fase de "sistema real de negocio".

- Login: `features/auth`, `authStore` (zustand + persist, incluye `role` y `puede_retirar_excedente`), `LoginPage`/`DashboardPage`. Tras login, `role=cajero` redirige a `/ventas` (no al Dashboard, que es vista exclusiva de admin — el cajero jamás la ve). `LoginPage` usa `SplitBrandScreen` (ver abajo) — ya no la Card centrada chica original.
- `components/SplitBrandScreen.tsx`: layout compartido de pantalla completa (sin sidebar de la app) para las dos pantallas fuera del flujo normal: `LoginPage` y `AbrirCajaSplash`. El panel izquierdo es **puramente de marca** — "MVP POS" + tagline fija "Punto de venta", siempre igual sin importar la pantalla ni el usuario (la marca es la marca) — más un `footer` opcional (`LiveClock`, y en apertura de caja también "Último cierre"). Lo que cambia por pantalla (la acción, la info del cajero) vive del lado derecho vía `children`, con el patrón "eyebrow" (texto chico en mayúsculas, `text-primary`, ej. "Apertura de caja"/"Acceso al sistema") arriba del título grande — nunca en el panel de marca. Todo con tamaños fluidos vía `clamp()`, crece con el monitor en vez de topar a un ancho fijo. Deliberadamente **no** se aplicó este mismo enfoque fluido al resto de la app (listados/formularios ya resueltos con `w-full` + breakpoints estándar) — meterlo en tablas de trabajo densas en datos rompería la densidad de información, que ahí es lo que importa.
- `components/LiveClock.tsx`: reloj/fecha en vivo (hora del navegador) reutilizado por ambas pantallas de `SplitBrandScreen`.
- `ProtectedLayout` gatea por completo la app para `role=cajero` mientras no exista ninguna caja abierta (`useCajaActual`): en vez de cualquier chrome, renderiza `AbrirCajaSplash` (`features/caja/components/AbrirCajaSplash.tsx`) — pantalla completa tipo kiosco, sin menú. El cajero llega a trabajar con su monto inicial ya contado; no tiene sentido dejarlo navegar antes de abrir caja. En cuanto la caja abre (por él o porque otro cajero/admin ya la tenía abierta), pasa a su propio chrome de kiosko (topbar, ver Seguridad y roles) — nunca al sidebar/Dashboard de admin, esos ni existen para este rol. Admin nunca se gatea así, siempre ve el Dashboard con sidebar.
  - Diseño de `AbrirCajaSplash` (a partir de un mockup del cliente, ahora sobre `SplitBrandScreen`): selector de **equipo** (`useEquiposDisponibles`, ver Multisucursal — con un solo equipo activo en la sucursal se autoselecciona y se muestra como etiqueta fija de solo lectura, sin pedir una decisión sin opciones reales) + input grande de monto con `$` + botones de monto rápido (500/1.500/1.000/3.000, azúcar de UI sin relación con ningún dato real, filtrados para no ofrecer un preset que ya de por sí supere `limite_efectivo_caja`) + hint de "Máx. {limite}" (dato real, oculto si no hay límite configurado) + "Último cierre" (dato real vía `caja_repository.get_ultima_cerrada_by_usuario`, campo `CajaActualOut.ultimo_cierre`) + reloj/fecha en vivo (solo hora del navegador, sin lógica de negocio), inyectados como `footer` de `SplitBrandScreen`. Se descartó del mockup original: "Turno" (no existe concepto de turnos en el sistema) — a diferencia de Terminal/Sucursal, sí llegaron a implementarse en Multisucursal.
  - Pendiente a futuro: disparar en ese mismo punto la activación de hardware (scanner de código de barras, terminal BBVA) — hoy no existe esa integración.
- Categorías y Subcategorías (§ catalogación real): `Subcategorías` **no tiene ruta ni página propia** — se gestiona desde `CategoriasPage`, botón "Subcategorías" por fila abre `SubcategoriasDialog` (`features/subcategorias`) con la lista/alta/edición de esa categoría, mismo patrón `TableCard`. `ProductoForm` agrega select en cascada Categoría→Subcategoría (la subcategoría se filtra por la categoría elegida, se resetea si la categoría cambia); al crear con subcategoría elegida, el campo SKU se oculta (se autogenera en el backend) — en edición el SKU sigue siendo siempre editable. `ProductosTable`/`InventarioPage` muestran el breadcrumb `Categoría > Subcategoría` cuando existe, si no cae al `categoria` directo histórico.
- Inventario (admin-only, antes visible a cualquier logueado): columna "Stock actual" muestra `stock_resultante` del movimiento (snapshot histórico), no el stock en vivo del producto — mostrar el stock actual ahí rompía la coherencia del historial en cualquier producto con más de un movimiento. Entrada/Salida con ícono y color (`text-success`/`text-destructive`) y cantidad con signo (`+60`/`-20`). Filtros: búsqueda por nombre/SKU, tipo (Entrada/Salida), rango Desde/Hasta.
- Caja: `features/caja` — `useCajaActual` devuelve `{ caja, efectivo_actual, limite_efectivo, excede_limite, ultimo_cierre }`, scopeada a "mi caja" en el backend. **`CajaPage` y la ruta `/caja` ya no existen** (se eliminaron por completo): toda la operación diaria del cajero (Registrar movimiento, historial de movimientos, Terminar turno) se consolidó dentro de `VentaKiosco` (ver Ventas abajo) — la barra de estado de la pantalla de venta ya mostraba monto inicial/ventas en efectivo/esperado, tener una pestaña "Caja" aparte con la misma info era pura redundancia. `AbrirCajaSplash` (apertura) no cambió: propaga `MontoInicialExcedeLimiteError` (400) si el monto inicial supera el límite, y `EquipoNoDisponibleError`/`EquipoOcupadoError` si el equipo no sirve.
  - Excedente de efectivo: ya no es un banner descartable. Apenas `excede_limite` pasa a `true`, `VentaKiosco` abre solo (sin acción del cajero) una pantalla completa — `Sheet` variante `full` (ver Sheet abajo), sin botón de cerrar y sin poder cerrarse por Esc/click-afuera mientras la caja siga excedida (`onOpenChange` ignora el intento de cierre a menos que ya se haya resuelto). El backend además bloquea de verdad cualquier venta nueva (cualquier `forma_pago`) mientras siga excedida — ver `docs/BACKEND.md`.
    - Diseño (a partir de mockups del cliente, dos estados según `puede_retirar_excedente`): split de dos columnas — izquierda eyebrow rojo "Cobro detenido" + titular grande (`font-black`) + explicación + política de la sucursal/hora de apertura en el pie; derecha desglose tabular (efectivo acumulado / máximo permitido / **monto a retirar** en grande) y, según el permiso, un botón "Confirmar retiro de \$X" o una caja "Requiere autorización". Colores/tipografía de los tokens existentes (`--destructive`, `font-black` de Geist), no el skin rojo/negro literal del mockup. Se descartó del mockup el aviso "enviado a [admin]" — no existe sistema de notificaciones, no se iba a fingir uno.
    - Al resolverse (retiro propio o detectado de otra sesión), otro `Sheet` `full` separado ("Retiro registrado") con el mismo split: izquierda resumen + botones "Imprimir comprobante"/"Volver a ventas" (`setVoucher(null)`); derecha `ComprobanteRetiro` (ver Impresión abajo). "Turno Matutino 09:00–15:00" del mockup se descartó (no existe turno, ver Multisucursal) — se usa Equipo + hora de apertura real.
  - `components/ui/sheet.tsx` (nuevo): mismo primitivo Radix que `dialog.tsx` (`Dialog` renombrado `Sheet`), variantes `left`/`right`/`full` (`full` cubre toda la pantalla con fade+zoom en vez de deslizar, para el bloqueo de excedente). Los `Sheet` del cajero (Productos, excedente) no se portan a `document.body` como un `Dialog` normal: `ProtectedLayout` expone su propio `<main>` como contenedor del portal (`container` prop de Radix + callback ref `setMainEl` + `transform-gpu`, que crea el "containing block" que necesita un `position: fixed` portado ahí para no ignorar ese contenedor) y lo pasa a las rutas hijas vía `useOutletContext` — así el appbar del kiosko queda siempre visible por encima, nunca tapado por el panel/pantalla.
- Usuarios (`features/usuarios`, `UsuariosPage`, admin-only): tabla de usuarios con switch para otorgar/quitar `puede_retirar_excedente` por cajero (el admin siempre puede, no tiene switch), más columna "Sucursal" (`sucursal_nombre`). Alta de cajero (`UsuarioForm`) exige elegir sucursal (`SelectField` poblado por `useSucursales`, activas únicamente) — mismo idioma que `CompraForm` usa para proveedor: `sucursal_id: z.number().nullable()` + `.superRefine` que exige no-null al enviar, no un simple `.min(1)` de string. Columna "Caja" muestra `usuario.caja_activa` (punto verde/gris, "Caja activa"/"Sin caja") — desde Multisucursal puede haber varios cajeros en verde a la vez (N cajas abiertas). Deliberadamente no es "está logueado": un login sin caja abierta no importa para el control de efectivo, que es lo que esto resuelve.
- Logout (`features/auth/hooks/useLogout.ts`): `clearSession()` solo en `onSuccess` (antes era `onSettled`, limpiaba la sesión local aunque el backend rechazara el logout). Si el usuario tiene su caja abierta, el backend responde 409 y `ProtectedLayout` muestra el mensaje de error bajo el botón "Cerrar sesión" sin desloguear localmente — evita que la UI muestre "deslogueado" mientras la cookie sigue viva en el servidor.
- Auditoría (`features/auditoria`, `AuditoriaPage`, admin-only, solo lectura): filtros por módulo (`entidad`) y rango de fechas, tabla con usuario/evento/detalle. Sin formularios — mismo criterio que `ReportesPage` (feature simple, sin `components/` propios más allá de la página).
- Configuración (`features/configuracion`, `ConfiguracionPage`, admin-only): formulario único para `limite_efectivo_caja`, `openpay_tope_por_orden`, `openpay_tope_diario` (todos opcionales — vacío significa "sin tope").
- Compras: `CompraForm` cambió el campo de texto libre "Proveedor" por un `SelectField` poblado desde `useProveedores` (solo proveedores activos).
- Proveedores (`features/proveedores`, `ProveedoresPage`, admin-only): mismo patrón CRUD que `categorias` (form + tabla + toggle con confirmación), campos nombre/contacto/teléfono/email/CLABE.
- Reglas de reorden (`features/reglas-reorden`, `ReglasReordenPage`, admin-only): alta fija el producto (no se puede reasignar al editar, solo desactivar y crear una nueva regla); edición permite cambiar proveedor/umbral/cantidad/costo estimado.
- Órdenes de reorden (`features/ordenes-reorden`, `OrdenesReordenPage`, admin-only): filtro por estado (default "Pendientes"), acciones "Aprobar y pagar" (con `AlertDialog` de confirmación explícita, menciona que es un pago real e irreversible) y "Rechazar". Estado `error` muestra el mensaje de OpenPay en la fila.
- Ventas: `VentasPage` bifurca por rol al inicio (hooks llamados sin condicionar, branch de retorno después — mismo patrón ya usado en `CajaPage` histórico): `role=cajero` → `VentaKiosco`; `role=admin` → historial/auditoría de siempre (`VentasTable` + `VentaDetalleDialog`, sin ningún selector de creación). El viejo modal "Nueva venta"/`VentaForm` para admin se eliminó junto con `ventaSchema.ts`: era código inalcanzable — el backend exige caja abierta para crear una venta y el admin nunca abre caja, ese botón nunca pudo funcionar.
  - `VentaKiosco` (`features/ventas/components/VentaKiosco.tsx`): pantalla de venta real del cajero, adaptada de un mockup de POS clásico a los tokens de diseño existentes (no se clonó la paleta del mockup). Input de código/SKU + Enter agrega (`listProductos({q, size:5})` + filtro exacto client-side sobre `sku` — no hizo falta un endpoint de búsqueda exacta dedicado). Tabla de ticket con `TableHeader` `sticky`, única región con scroll interno en pantallas grandes (`lg:` — en pantallas chicas se apila y el scroll lo asume `main`); input/footer/panel de cobro quedan siempre fijos. Panel derecho: forma de pago + pagó-con/cambio inline (sin modal aparte) + Cobrar (`F12`) + Cancelar ticket. Atajos de teclado reales (Enter agrega, F12 cobra, Esc cancela), deshabilitados mientras cualquier dialog/sheet interno del propio `VentaKiosco` esté abierto.
  - Consolidación con Caja (ver arriba): la barra de estado de `VentaKiosco` reemplaza a la vieja `CajaPage` — Monto inicial/Ventas en efectivo/Esperado en caja + botones Movimientos (registrar entrada/salida + historial, un solo dialog en vez de dos) / Ventas del día (historial de ventas del propio turno) / Terminar turno (cierra caja y encadena logout: el backend ya exige caja cerrada para permitir el logout, así que encadenarlos evita cualquier estado intermedio confuso para el cajero).
  - `VentaDetalleDialog` (`features/ventas/components/`): se extrajo de `VentasPage` (antes vivía inline ahí) para reusarlo también desde "Ventas del día" en `VentaKiosco`, evitando duplicar el JSX del detalle en dos lugares.
- Corte de caja de emergencia desde `UsuariosPage`: el botón "Cerrar caja" vive directo en la fila del cajero con `caja_activa` en `UsuariosTable` — reutiliza `CierreCajaForm` de `features/caja`, pero contra `useCajaDeUsuario(usuarioId)`/`useCerrarCajaDeUsuario()` (`GET`/`POST /usuarios/{id}/caja...`, scopeados al cajero del path, no al admin que llama) en vez de `useCajaActual`/`useCerrarCaja` — esos quedaron atados al usuario que llama tras el rescoping de Multisucursal, y el admin nunca tiene caja propia. `useCerrarCajaDeUsuario` invalida también `['usuarios']` para que el punto verde se apague al instante sin recargar.
- Retirar excedente desde `UsuariosPage` (mismo botón-por-fila que "Cerrar caja", `useCajaDeUsuario`/`useRetirarExcedenteDeUsuario` en vez de `useCerrarCajaDeUsuario`): a diferencia del corte de emergencia, este **no** se enmarca como excepción — es cuadre de caja rutinario, así que el dialog no dice "úsalo solo si..." como el de cerrar caja. Reusa `VoucherRetiroDialog` de `features/caja` (mismo componente que usa el cajero al retirar la suya desde `VentaKiosco`) para el comprobante.
  - El comprobante tiene que salir impreso en la caja física donde está el efectivo (`VentaKiosco`), no en el navegador del admin que dispara la acción — si el admin retira remoto, imprimir desde su propia máquina no sirve de nada. `useCajaActual` agrega `refetchInterval: 15_000` (antes no había polling — el cajero solo se enteraba de cambios de otra sesión si algo propio disparaba un refetch) y `VentaKiosco` trackea la transición `excede_limite: true → false` con un ref (`excedePrevRef`): si se resuelve sin pasar por su propio `handleRetirarExcedente` (que ya muestra el voucher al instante desde la respuesta de la mutación), pide `getUltimoRetiroExcedente()` y muestra ese comprobante ahí mismo — deduplicado contra `shownVoucherIdRef` para no mostrarlo dos veces cuando sí fue el propio cajero quien lo retiró.
- Multisucursal (diseño completo y rationale en `docs/MULTISUCURSAL.md`):
  - `features/sucursales`/`features/equipos` + `pages/SucursalesPage.tsx` (admin-only, en `/sucursales`): copia exacta del patrón `proveedores` (sucursales) y `subcategorias` (equipos — botón "Equipos" por fila abre `EquiposDialog`, mismo esqueleto que `SubcategoriasDialog`).
  - `stores/sucursalActivaStore.ts` (zustand + persist, solo `sucursalId`) + `components/SucursalActivaSelector.tsx`: selector visible para `admin` en Productos/Inventario/Compras/Reglas/Órdenes de reorden — pantallas que muestran/mutan stock, y el admin no pertenece a ninguna sucursal. Se autoselecciona la primera sucursal activa si no hay ninguna elegida. El `cajero` nunca ve este selector — su `sucursal_id` se resuelve siempre en el servidor (`resolve_sucursal_id`), el valor del store simplemente se ignora si viaja.
  - `useProductos`/`useMovimientos`/`useCompras`/`useReglasReorden`/`useOrdenesReorden` leen `sucursalActivaStore` internamente (no cada pantalla se lo pasa por props) e incluyen el `sucursalId` en su `queryKey` — cambiar de sucursal en el selector invalida y recarga automáticamente.
  - `services/productoService.ts`: `Producto` (catálogo puro) vs. `ProductoConStock` (+ `stock`, solo para el listado/detalle de `/productos`) — refleja el mismo split que el backend (`ProductoOut`/`ProductoStockOut`), porque el stock ya no es un campo fijo del producto sino de producto×sucursal.
  - `DashboardPage`/`ReportesPage`: la tarjeta "Estado de caja"/"Resumen de caja" (singulares) se reemplazaron por un agregado ("N cajas abiertas, $X esperado en total") para admin y una lista (una entrada por caja abierta) respectivamente, sobre `GET /reportes/cajas-abiertas` — con N cajas no hay "la actual" singular. `DashboardPage` ya es puramente admin (ver Seguridad y roles) — las ramas condicionales que antes escopeaban esta tarjeta a la caja del `cajero` se quitaron, ese rol nunca visita la página.
